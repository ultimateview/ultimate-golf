import { YardsLabelPipe } from './yards-label.pipe';

describe('YardsLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new YardsLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
