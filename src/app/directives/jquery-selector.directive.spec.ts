import { JquerySelectorDirective } from './jquery-selector.directive';

describe('JquerySelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new JquerySelectorDirective();
    expect(directive).toBeTruthy();
  });
});
