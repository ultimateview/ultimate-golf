import { PackedObjectDirective } from './packed-object.directive';

describe('PackedObjectDirective', () => {
  it('should create an instance', () => {
    const directive = new PackedObjectDirective();
    expect(directive).toBeTruthy();
  });
});
